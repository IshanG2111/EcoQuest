import { AppLoader } from '@/components/ui/app-loader';

export default function Loading() {
  return (
    <AppLoader
      title="Booting EcoQuest"
      subtitle="Warming up your sustainability dashboard..."
    />
  );
}
